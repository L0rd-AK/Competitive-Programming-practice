#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
#include <string.h>
#include <bits/stdc++.h>
using namespace std;

#define in cin
#define out cout
#define end endl
#define f(j,n) for(int i=j;i<n;i++)

void reverseStr(string str)
{
  for (int i = str.length() - 1; i >= 0; i--)
    out << str[i];
}
int main() 
{
  long long int t,k,l,m,n,ans,sum=0,x,x1,y1,x2,y2,max_num,min_h;
  char ch;
  string s;
  vector<int> a,b;
  in>>n;
  if(n==2) {out<<2;return 0;}
  out<<1;
  
  return 0;
}
